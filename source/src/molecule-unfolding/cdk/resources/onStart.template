#!/usr/bin/env bash

sudo -u ec2-user -i <<EOS

TMPDIR=$(mktemp -d)
cd "$TMPDIR"

aws s3 cp s3://braketnotebookcdk-notebooklccs3bucketb3089b50-1w1epzvg1km1k/notebook/braket-notebook-lcc.zip braket-notebook-lcc.zip
unzip braket-notebook-lcc.zip
./install.sh

nohup rm -fr "$TMPDIR" &

cd /home/ec2-user/SageMaker
rm -rf *
aws s3 cp s3://amazon-braket-gcr-qc-sol-common/molecule-unfolding-code/latest/molecule-unfolding-code.zip .
unzip molecule-unfolding-code.zip
EOS

exit 0
